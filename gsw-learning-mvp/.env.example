# =============================================================================
# GSW 學習 MVP 環境配置文件
# =============================================================================
# 
# 使用說明：
# 1. 複製此文件為 .env：cp .env.example .env
# 2. 填入您的實際 API 密鑰和配置值
# 3. 確保 .env 文件不被提交到版本控制系統（已在 .gitignore 中排除）
#
# =============================================================================

# =============================================================================
# LLM API 配置
# =============================================================================

# -----------------------------------------------------------------------------
# Google Gemini API 配置（必填）
# -----------------------------------------------------------------------------
# 用途：語義提取、實體對齊、衝突解決等核心 AI 任務
# 獲取方式：https://makersuite.google.com/app/apikey
# 注意：請妥善保管您的 API 密鑰，不要分享給他人
GEMINI_API_KEY=your_gemini_api_key_here

# -----------------------------------------------------------------------------
# OpenAI API 配置（可選，用於 Embedding）
# -----------------------------------------------------------------------------
# 用途：情景摘要的向量化和語義相似度計算
# 獲取方式：https://platform.openai.com/api-keys
# 注意：如果不使用 OpenAI Embedding，可以留空
OPENAI_API_KEY=your_openai_api_key_here

# =============================================================================
# LLM 模型參數配置
# =============================================================================

# Gemini 模型名稱
# 可選值：gemini-pro, gemini-pro-vision, gemini-1.5-pro, gemini-1.5-flash
# 建議：gemini-1.5-flash 速度快且成本低，適合開發測試
GEMINI_MODEL_NAME=gemini-1.5-flash

# OpenAI Embedding 模型
# 用途：文本向量化
# 推薦：text-embedding-ada-002（性價比高）或 text-embedding-3-small（更新版本）
OPENAI_EMBEDDING_MODEL=text-embedding-ada-002

# 模型溫度（Temperature）
# 範圍：0.0 - 1.0
# 0.0：回應更確定、一致
# 1.0：回應更有創造性、多樣化
# 建議：知識提取任務使用較低值（0.3-0.5）
MODEL_TEMPERATURE=0.5

# 最大輸出 Token 數量
# 用途：控制 LLM 回應的最大長度
# 建議值：1024-4096，根據任務複雜度調整
MAX_TOKENS=2048

# =============================================================================
# 系統運行配置
# =============================================================================

# 日誌級別
# 可選值：DEBUG, INFO, WARNING, ERROR, CRITICAL
# DEBUG：詳細調試信息，適用於開發階段
# INFO：一般運行信息，適用於正常運行
# WARNING：警告信息
# ERROR：錯誤信息
# CRITICAL：嚴重錯誤信息
LOG_LEVEL=INFO

# 日誌格式
# 可選值：simple, detailed, json
# simple：簡單格式，只顯示時間和消息
# detailed：詳細格式，包含文件名和行號
# json：JSON 格式，適合日誌收集系統
LOG_FORMAT=detailed

# 日誌輸出目錄
# 用途：存儲日誌文件的目錄路徑
LOG_DIRECTORY=./logs

# =============================================================================
# 數據存儲配置
# =============================================================================

# 向量數據庫持久化目錄
# 用途：Chroma 向量數據庫的存儲位置
CHROMA_PERSIST_DIRECTORY=./chroma_db

# 工作空間記憶文件路徑
# 用途：存儲 GSW 框架的語義狀態
MEMORY_FILE_PATH=./memory.json

# 提示詞文件目錄
# 用途：存儲 LLM 提示詞模板
PROMPTS_DIRECTORY=./data/prompts

# 數據文件目錄
# 用途：存儲輸入的原始數據文件
DATA_DIRECTORY=./data

# =============================================================================
# 文本處理配置
# =============================================================================

# 文本分塊大小（字符數）
# 用途：每個 chunk 的目標大小
# 建議值：500-2000，根據文本類型調整
CHUNK_SIZE=1000

# 分塊重疊大小（字符數）
# 用途：相鄰 chunks 之間的重疊，用於保留上下文
# 建議值：chunk_size 的 10-20%
CHUNK_OVERLAP=100

# 分塊策略
# 可選值：fixed, semantic, paragraph
# fixed：固定大小分塊
# semantic：基於語義邊界分塊
# paragraph：基於段落邊界分塊
CHUNKING_STRATEGY=fixed

# =============================================================================
# 查詢優化配置
# =============================================================================

# Top-K 返回數量
# 用途：語義搜索返回的最相關結果數量
TOP_K_RESULTS=5

# 相似度閾值
# 範圍：0.0 - 1.0
# 用途：只返回相似度高於此值的結果
# 建議值：0.6-0.8
SIMILARITY_THRESHOLD=0.7

# =============================================================================
# 中文環境配置
# =============================================================================

# 系統語言環境
# 用途：確保正確處理中文字符
LANG=zh_TW.UTF-8
LC_ALL=zh_TW.UTF-8

# Python 輸出編碼
# 用途：確保中文日誌和錯誤信息正確顯示
PYTHONIOENCODING=utf-8

# =============================================================================
# 開發和測試配置
# =============================================================================

# 調試模式開關
# True：啟用詳細日誌和調試信息
# False：生產模式，減少日誌輸出
DEBUG_MODE=False

# 測試模式配置
# True：使用模擬數據和 Mock API 調用
# False：使用真實 API 調用
TEST_MODE=False

# 屬性測試迭代次數
# 用途：每個屬性測試的迭代次數
# 建議值：開發時 10-50，CI/CD 時 100+
PROPERTY_TEST_ITERATIONS=10

# API 請求重試次數
# 用途：API 調用失敗時的重試次數
API_RETRY_COUNT=3

# API 請求超時時間（秒）
# 用途：單次 API 請求的超時時間
API_TIMEOUT=30

# =============================================================================
# 效能優化配置
# =============================================================================

# 批次處理大小
# 用途：批量處理時每批的數量
BATCH_SIZE=10

# 並發請求數量
# 用途：最大同時進行的 API 請求數
MAX_CONCURRENT_REQUESTS=5

# 緩存啟用開關
# True：啟用結果緩存，減少重複 API 調用
# False：禁用緩存
ENABLE_CACHE=True

# 緩存過期時間（秒）
# 用途：緩存結果的有效期
CACHE_TTL=3600